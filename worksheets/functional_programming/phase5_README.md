# Phase 5: Module Imports & Strategies

## Your task

- Use aliasing for the `products` module
- Import `show_cart` function directly from `display`
- Import `cart` module with an alias
- Implement `demo()` to create a cart, add a product, and display the cart
- Comment on your import choices

## Tips

- Experiment with import styles and observe namespaces
- Try intentionally bad imports and fix them by using aliasing
